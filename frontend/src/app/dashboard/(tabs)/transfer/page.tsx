"use client";

import TransferForm from "@/components/forms/TransferForm";

export default function TransferPage() {
  return <TransferForm onSuccess={() => { /* optional: nothing; layout dinleyecek */ }} />;
}
